apiVersion: v1
kind: Pod
metadata:
  name: demo-rw-layer-1
spec:
  containers:
  - name: loader-layer-1
    image: lustrecsi.azurecr.io/demo-multi-rw-loader:latest
    volumeMounts:
    - name: storage-isolation
      mountPath: /test
  volumes:
  - name: storage-isolation
    persistentVolumeClaim:
      claimName: loader-pvc
  imagePullSecrets:
  - name: defregistrykey